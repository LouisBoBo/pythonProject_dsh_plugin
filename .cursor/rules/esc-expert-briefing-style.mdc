---
description: ESC 专家简报必须有表有图有真实数据（领导扫读风），禁止大段散文
globs: dsh-esc/assets/experts/**/*.md,dsh-esc/assets/skills/**/*.md,dsh-esc/assets/scenes/**/*.json,dsh-esc/src/prompt.ts,dsh-esc/src/adapters/mcp_chart.ts
alwaysApply: false
---

# 专家场景简报风格（已验收，领导喜欢）

改 `dsh-esc` 专家 / 技能 / 场景卡 / 出图时，**必须**保持 0.1.15 这版「PCB 生产运营分析」口径。用户原话：有图有数据，领导就喜欢这样的。

## 交付形态

- **能进表就进表**：风险、产能、产线、缺陷、库存、建议。解释用短段落，每段一个判断。
- **一次分析 3～5 张图**，类型尽量不重复（面积/折线、双轴、柱状、漏斗/饼、雷达、条形）。图紧贴对应表，用 Markdown `![](url)`。
- **数字只来自已启用连接器**（MES `zr_esc_mes_query`）。失败写 `error.code` 标盲区，禁止编造。
- **PCB 专家落点**：SMT → DIP → 组装 → 测试；达产先核单位（套/点）；缺陷落到工序，不写空话。

## 反例

```text
❌ 大片连续叙述、只出 1 张折线、达产率不核口径、用产出跌幅冒充 OEE
✅ 风险一览表 + 多图对照 + 工序含义 + 可验证建议
```

不要把简报改回「普通看板汇总」或散文汇报。
